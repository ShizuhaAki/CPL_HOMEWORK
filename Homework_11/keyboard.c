#include <stdio.h>
#include <stdlib.h>

int main()
{
    int Q;
    scanf("%d", &Q);
    while (Q--) {
        char s[2000005] = {0};
        scanf("%s", s);
        if (s[0] == '?') {
            // ²éÑ¯
        } else {
            // ×·¼Ó
        }
    }
    return 0;
}